import { PassDownWidthDirective } from './pass-down-width.directive';

describe('PassDownWidthDirective', () => {
  it('should create an instance', () => {
    const directive = new PassDownWidthDirective();
    expect(directive).toBeTruthy();
  });
});
